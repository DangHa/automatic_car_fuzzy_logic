{
  "name": "automatic_car",
  "version": "1.0.0",
  "description": "using fuzzy set",
  "main": "index.js",
  "scripts": {
    "start": "nodemon src/server/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "ha_dang",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  }
}
